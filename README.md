1. When we write child component within a parent component
   For example:
   <Parent>
   asdfasdf
   </Parent>

   an error msg will show that property 'children; does not exist on type....

   If we define a component using React.FC this propblem will go away
